# 🛏️ Mattress Shop — Laravel Project

Це магазин матраців, розроблений для демонстрації навичок бекенд-розробки на фреймворку Laravel.

## 🚀 Основний функціонал

-   **Каталог товарів**: Динамічне виведення матраців із бази даних SQLite.
-   **Система замовлень**: Форма оформлення замовлення з передачею даних через POST-запити та індивідуальною сторінкою успіху.
-   **Адмін-панель (CRUD)**: Повний цикл керування складом — створення нових позицій та видалення існуючих.
-   **Безпека**: Використання CSRF-захисту для всіх форм та Mass Assignment Protection (fillable) у моделях.

## 🛠 Технологічний стек

-   **Backend**: PHP 8.4, Laravel
-   **Database**: SQLite (Eloquent ORM)
-   **Frontend**: Blade Templates, Tailwind CSS
-   **Інструменти**: Git, Composer, Artisan CLI

## 📸 Скріншоти проекту

### Головна сторінка та оформлення

![Оформлення замовлення](https://github.com/Yurii-Masniuk/laravel-mattress-shop/raw/main/public/screenshots/checkout.png)

### Адмін-панель

![Додавання товару](https://github.com/Yurii-Masniuk/laravel-mattress-shop/raw/main/public/screenshots/admin.png)

## ⚙️ Як запустити локально

1. **Клонувати репозиторій**:

```bash
    git clone https://github.com/Yurii-Masniuk/laravel-mattress-shop.git
```

2. **Встановлення залежностей**:

```bash
   composer install
```

3. **Налаштування оточення**:

```bash
   cp .env.example .env
   php artisan key:generate
```

4. **Підготувати базу даних (SQLite)**:

```bash
   touch database/database.sqlite
   php artisan migrate
```

5. **Запустити проект**:

```bash
   php artisan serve
```

## 📂 Ключові файли для перегляду

-   `routes/web.php` — опис усіх маршрутів (Front та Admin).
-   `app/Http/Controllers/AdminController.php` — основна логіка: виведення товарів, збереження замовлень та CRUD адмінки.
-   `app/Models/Mattress.php` — модель із захистом `$fillable` для безпечного додавання даних.
-   `database/migrations/` — структура таблиць бази даних SQLite.
-   `resources/views/welcome.blade.php` — головна сторінка каталогу.
-   `resources/views/admin/` — шаблони інтерфейсу адміністратора.
